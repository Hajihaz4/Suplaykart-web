packages:
  - "apps/*"
  - "packages/*"

# Native postinstall scripts approved to run (pnpm ignores them by default).
allowBuilds:
  '@clerk/shared': true
  esbuild: true
  sharp: true
  unrs-resolver: true
