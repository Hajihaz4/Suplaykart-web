# @suplaykart/ui (placeholder)

Shared component library (~14 primitives + ~12 composites) + the Tailwind preset
extracted from the legacy design tokens. **Populated in Phase 1B** when UI work
begins (see `docs/guides/component-inventory.md`).

README-only for now so it stays out of the build graph.
