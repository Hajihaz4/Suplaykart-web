# @suplaykart/core (placeholder)

Framework-agnostic domain logic — order state machine, inventory reservation,
pricing/bill engine, coupon validation, RBAC policy. **Populated in Phase 1B+.**

Kept as a README-only directory for now so it does not enter the pnpm/Turbo
build graph until it has real code.
