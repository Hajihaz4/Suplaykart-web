# @suplaykart/validators (placeholder)

Zod schemas shared client/server (derived from the DB schema via `drizzle-zod`).
**Populated in Phase 1B+** when server actions and forms are built.

README-only for now so it stays out of the build graph.
