# @suplaykart/auth (placeholder)

Clerk wrappers, session/role helpers, and the `authorize(user, action, resource)`
policy layer. **Populated in Phase 1B+.**

In Phase 1A, Clerk is wired directly in `apps/web` (ClerkProvider + middleware).
README-only for now so it stays out of the build graph.
