# packages/db reads DATABASE_URL for drizzle-kit migrate / push / studio.
# `drizzle-kit generate` does NOT need a live connection.
DATABASE_URL="postgresql://USER:PASSWORD@HOST-pooler.REGION.aws.neon.tech/DB?sslmode=require"
