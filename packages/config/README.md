# @suplaykart/config (placeholder)

Shared config — Tailwind preset, ESLint, and `tsconfig` bases. **Populated in
Phase 1B** alongside `@suplaykart/ui`.

In Phase 1A, the shared TS base lives at the repo root (`tsconfig.base.json`).
README-only for now so it stays out of the build graph.
